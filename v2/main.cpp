#include <iostream>
#include "pixel.h"
#include "image.h"

using namespace std;

int main() {
    freopen("stack.in", "r", stdin);
    // freopen("stack.out", "w", stdout);

    unsigned short n, x, y;
    cin >> n >> y >> x;

    Image images[n];

    








    cout << n << endl;

    return 0;
}